~/ full0 - test the first iteration of the parser.
 / Here, we just have variable declarations, operators, functions, and control
 / flow. No loops or ifs, or imports, or anything.
 /~

foo(x: int, y: char): int {
    z: int = x + y;
    return z * 2 - (3 +  5);
}

bar(): int {
    return foo(5, 6 - 7);
}

loops() {
    loop (3 < 5) {
        ~ effective infinite loop ~
    }
    loop (5) {
        ~ loop five times ~
    }
}
